pipeline {
	agent any

	environment {
		mavenHome = tool 'apache-maven-3.9.6'
	}

	tools {
		jdk 'java-11'
	}

	stages {

		stage('Build'){
			steps {
				bat "mvn clean install -DskipTests"
			}
		}

		stage('Test'){
			steps{
				bat "mvn test"
			}
		}
	}
}
